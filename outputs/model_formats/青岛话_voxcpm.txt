# 青岛话 — VoxCPM 适配文件

> 方言组：山东话（分支） | 谱系：胶辽官话
> 生成模型：VoxCPM2 (openbmb/VoxCPM2)
> 格式：Control Instruction + Target Text

---

## 默认 Control Instruction

```
山东话，中年男性，豪爽语气，正常语速
```

## 完整输入示例

```
(山东话，中年男性，豪爽语气，正常语速)短视频
```

## Python 调用示例

```python
from voxcpm import VoxCPM
import soundfile as sf

model = VoxCPM.from_pretrained("openbmb/VoxCPM2", load_denoiser=False)

wav = model.generate(
    text="(山东话，中年男性，豪爽语气，正常语速)短视频",
    cfg_value=2.0,
    inference_timesteps=10,
    seed=42,
)
sf.write("青岛话_voxcpm.wav", wav, model.tts_model.sample_rate)
```

## 代表性词条（前 5 条）

| 普通话 | 方言 |
|--------|------|
| 短视频 | 短视频 |
| 直播 | 直播 |
| 带货 | 带货 |
| 打赏 | 打赏 |
| 弹幕 | 弹幕 |

---

> 完整词条见 `references/dialects/24_qingdao.md`
