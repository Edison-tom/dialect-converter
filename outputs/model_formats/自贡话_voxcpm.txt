# 自贡话 — VoxCPM 适配文件

> 方言组：四川话（分支） | 谱系：西南官话·仁富片
> 生成模型：VoxCPM2 (openbmb/VoxCPM2)
> 格式：Control Instruction + Target Text

---

## 默认 Control Instruction

```
四川方言，中年男性，慵懒语气，语速偏慢
```

## 完整输入示例

```
(四川方言，中年男性，慵懒语气，语速偏慢)打雷
```

## Python 调用示例

```python
from voxcpm import VoxCPM
import soundfile as sf

model = VoxCPM.from_pretrained("openbmb/VoxCPM2", load_denoiser=False)

wav = model.generate(
    text="(四川方言，中年男性，慵懒语气，语速偏慢)打雷",
    cfg_value=2.0,
    inference_timesteps=10,
    seed=42,
)
sf.write("自贡话_voxcpm.wav", wav, model.tts_model.sample_rate)
```

## 代表性词条（前 5 条）

| 普通话 | 方言 |
|--------|------|
| 打雷 | 打雷 |
| 闪电 | 扯火闪 |
| 厨房 | 灶房 |
| 厕所 | 茅斯 |
| 筷子 | 筷子 |

---

> 完整词条见 `references/dialects/19_zigong.md`
